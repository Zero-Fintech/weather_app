import 'package:flutter/material.dart';

class WeatherDetailsErrorWidget extends StatelessWidget {
  const WeatherDetailsErrorWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
