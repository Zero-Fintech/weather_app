import 'package:flutter/material.dart';

class WeatherDetailsLoadedWidget extends StatelessWidget {
  const WeatherDetailsLoadedWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
